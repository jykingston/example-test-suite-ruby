require "spec_helper"

describe "Login" do
  25.times do
    it "should authenticate users" do
      expect(2).to eq(2)
    end

    it "should restrict access" do
      expect(2).to eq(2)
    end
  end
end
