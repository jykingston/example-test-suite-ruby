require "spec_helper"

describe "Simple integers" do
  100.times do
    it "should equal themselves" do
      expect(2).to eq(2)
    end

    it "should equal themselves" do
      expect(2).to eq(2)
    end

    # it "random" do
    #   expect(rand(1)).to eq(rand(2))
    # end
  end
end
